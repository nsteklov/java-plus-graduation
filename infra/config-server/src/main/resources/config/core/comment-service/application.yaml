server:
  port: 0

logging:
    level:
        org:
            hibernate:
                SQL: WARN
            springframework:
                web: INFO
        ru:
            practicum: DEBUG
management:
    endpoint:
        health:
            show-details: always
    endpoints:
        web:
            exposure:
                include: health,info,metrics,prometheus
spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: ${SPRING_DATASOURCE_PASSWORD:comments}
        url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5437/comments}
        username: ${SPRING_DATASOURCE_USERNAME:comments}
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
                jdbc:
                    batch_size: 10
        show-sql: false

